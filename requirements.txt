# Vercel installs these for the serverless function (api/index.py).
# Demo brain runs on the standard library alone; these power the optional upgrades:
anthropic>=0.40        # live brain, when ANTHROPIC_API_KEY is set
elevenlabs>=1.0        # spoken replies via /api/chat {action:"speak"}, when ELEVENLABS_API_KEY is set
deepgram-sdk>=3.0      # speech-to-text via /api/chat {action:"transcribe"}, when DEEPGRAM_API_KEY is set
