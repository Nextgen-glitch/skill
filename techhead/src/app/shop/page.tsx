import { ComingSoon } from "@/components/ComingSoon";

export const metadata = { title: "Shop — TechHead Electronics" };

export default function ShopPage() {
  return (
    <ComingSoon
      title="The full shop is"
      blurb="We're wiring up the complete catalog with filters, search and product pages next. For now, explore the featured picks on the homepage."
    />
  );
}
