import { ComingSoon } from "@/components/ComingSoon";

export const metadata = { title: "Admin — TechHead Electronics" };

export default function AdminPage() {
  return (
    <ComingSoon
      title="The seller dashboard is"
      blurb="The admin panel to upload and manage products is the final milestone — building it next."
    />
  );
}
